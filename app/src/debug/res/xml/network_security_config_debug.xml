<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: allows cleartext HTTP to the emulator's host loopback for local
     backend testing. Never merged into release (separate source set). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
